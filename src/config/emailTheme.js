const path = require("path");

const brandConfig = {
  companyName: "Backend Ledger",
  primaryColor: "#0f172a",
  accentColor: "#2563eb",
  logoUrl: path.join(__dirname, "../assets/Safe_ledger_logo.png"),
  supportEmail: "support@backendledger.com",
};

module.exports = brandConfig;
